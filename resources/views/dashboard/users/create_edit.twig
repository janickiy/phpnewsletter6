{% extends 'templates/app.twig' %}

{% block content %}

    <div class="row-fluid">
        <div class="col">

            <!-- Widget ID (each widget will need unique ID)-->
            <div class="jarviswidget jarviswidget-color-blueDark" data-widget-editbutton="false">

                <!-- widget div-->
                <div>

                    <form id="tmplForm"  action="{{ user is defined ? path_for('admin.users.update') : path_for('admin.users.store') }}" method="{{ user is defined ? 'PUT' : 'POST' }}" class="smart-form">

                        {{ csrf.field | raw }}

                        {% if user %}<input type="hidden" name="id" value="{{ user.id }}">{% endif %}

                        <header>
                            *-обязательные поля
                        </header>

                        <fieldset>

                            <section>
                                <label for="name" class="label">Имя*</label>
                                <label class="input">
                                    <input type="text" maxlength="255" placeholder="Имя" name="name" value="{{ user is defined ? user.name : old.name }}" id="name">
                                </label>

                                {% if errors.name %}
                                    <span class="text-danger">{{ errors.name | first }}</span>
                                {% endif %}

                            </section>

                            <section>
                                <label for="login" class="label">Логин*</label>
                                <label class="input">
                                    <input type="text" maxlength="255" placeholder="Логин" name="login" value="{{ user is defined ? user.login : old.login }}" id="login">
                                </label>

                                {% if errors.login %}
                                    <span class="text-danger">{{ errors.login | first }}</span>
                                {% endif %}

                            </section>

                            <section>
                                <label for="description" class="label">Описание</label>
                                <label class="textarea textarea-resizable">
                                    <textarea rows="3" class="custom-scroll" placeholder="Описание" name="description"  id="description">{{ user is defined ? user.description: old.description}}</textarea>
                                </label>
                                {% if errors.description %}
                                    <span class="text-danger">{{ errors.description | first }}</span>
                                {% endif %}

                            </section>

                            <section>
                                <label for="role" class="label">Роль*</label>
                                <label class="select">
                                    <select id="role" name="role">
                                        <option>Выберите</option>
                                        <option value="admin" {{ user.role == 'admin' or old.role == 'admin' ? 'selected="selected"' : '' }}>Админ</option>
                                        <option value="moderator" {{ user.role == 'moderator' or old.role == 'moderator' ? 'selected="selected"' : '' }}>Модератор</option>
                                        <option value="editor" {{ user.role == 'editor' or old.role == 'editor' ? 'selected="selected"' : '' }}>Редактор</option>
                                    </select> <i></i>
                                </label>
                                {% if errors.role %}
                                    <span class="text-danger">{{ errors.role | first }}</span>
                                {% endif %}
                            </section>

                            <section>
                                <label for="password" class="label">Пароль</label>
                                <label class="input">
                                    <input type="password" maxlength="255" placeholder="пароль" name="password" value="{{ user is defined ? user.password : old.password }}" id="password">
                                </label>

                                {% if errors.password %}
                                    <span class="text-danger">{{ errors.password | first }}</span>
                                {% endif %}

                            </section>

                            <section>
                                <label for="password_again" class="label">Павтор пароля</label>
                                <label class="input">
                                    <input type="password" maxlength="255" placeholder="павтор пароля" name="password_again" value="{{ user is defined ? template.password_again : old.password_again }}" id="password_again">
                                </label>

                                {% if errors.password_again %}
                                    <span class="text-danger">{{ errors.password_again | first }}</span>
                                {% endif %}

                            </section>

                            <footer>
                                <button type="submit" class="btn btn-primary">
                                    Отправить
                                </button>
                                <a class="btn btn-default" href="{{ path_for('admin.users.index') }}">
                                    Назад
                                </a>
                            </footer>

                        </fieldset>

                    </form>

                </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block js %}


{% endblock %}