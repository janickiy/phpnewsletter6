{% extends 'templates/app.twig' %}

{% block content %}

    <div class="row-fluid">
        <div class="col">

            <!-- Widget ID (each widget will need unique ID)-->
            <div class="jarviswidget jarviswidget-color-blueDark" data-widget-editbutton="false">

                <!-- widget div-->
                <div>

                    <p>*-обязательные поля</p>



                    <form action="{{ template is defined ? path_for('admin.template.edit') : path_for('admin.template.create') }}"  method="{{ template is defined ? 'put' : 'post' }}" class="form-horizontal">

                    </form>



                </div>
            </div>
        </div>
    </div>

{% endblock %}